# 运行

## 说明

启用 mod_xml_curl，用于sip 用户注册

启用 mod_erlang_event，用于媒体

## 步骤

### erlang

编译期

```sh
需启用 mod_erlang_event
```

运行期

```sh
sudo sed -i '/mod_erlang_event/s/<!-- *\(.*mod_erlang_event.*\) *-->/\1/' /usr/local/etc/freeswitch/autoload_configs/modules.conf.xml
cat /usr/local/etc/freeswitch/autoload_configs/modules.conf.xml | rg mod_erlang_event
```

配置节点

```sh
/usr/local/etc/freeswitch/autoload_configs/erlang_event.conf.xml
```

```xml
<configuration name="erlang_event.conf" description="Erlang Socket Client">
  <settings>
    <param name="listen-ip" value="0.0.0.0"/>
    <param name="listen-port" value="8031"/>
    <!-- longname 的 value 里面必须带 ip -->
    <param name="nodename" value="fs@$${erlang_bind_ip}"/>
    <param name="cookie" value="123456"/>
    <!-- 短名字不支持 ip, 所以用 long name -->
    <param name="shortname" value="false"/>
    <param name="encoding" value="binary"/>
  </settings>
</configuration>
```

### curl

```sh
sed -i '/mod_xml_curl/s/<!-- *\(.*mod_xml_curl.*\) *-->/\1/' /usr/local/etc/freeswitch/autoload_configs/modules.conf.xml
cat /usr/local/etc/freeswitch/autoload_configs/modules.conf.xml | rg mod_xml_curl
```

```sh
/usr/local/etc/freeswitch/autoload_configs/xml_curl.conf.xml
```

```sh
<configuration name="xml_curl.conf" description="cURL XML Gateway">
  <bindings>
    <binding name="directory">
      <param name="gateway-url" value="http://$${erlang_bind_ip}:4000/api/fetch/directory" bindings="directory"/>
    </binding>
  </bindings>
</configuration>
```

## 测试

```sh
# 需要 root
freeswitch -nc -nonat

# 用 event socket
# 连接本地，不需要配置
fs_cli

# 查看版本
fs_cli -x version
```

```sh
erl -name aaa@127.0.0.1 -setcookie 123456
net_kernel:connect_node('fs@xx.xx.xx.xx').
nodes(hidden).
```
